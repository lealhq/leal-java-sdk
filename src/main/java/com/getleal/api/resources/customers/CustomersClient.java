/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.getleal.api.resources.customers;

import com.getleal.api.core.ClientOptions;
import com.getleal.api.core.RequestOptions;
import com.getleal.api.resources.customers.requests.CreateCustomersRequest;
import com.getleal.api.resources.customers.requests.GetCustomersRequest;
import com.getleal.api.resources.customers.requests.ListCustomersRequest;
import com.getleal.api.resources.customers.requests.UpdateCustomersRequest;
import com.getleal.api.resources.customers.types.CreateCustomersResponse;
import com.getleal.api.resources.customers.types.GetCustomersResponse;
import com.getleal.api.resources.customers.types.ListCustomersResponse;
import com.getleal.api.resources.customers.types.UpdateCustomersResponse;

public class CustomersClient {
    protected final ClientOptions clientOptions;

    private final RawCustomersClient rawClient;

    public CustomersClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new RawCustomersClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public RawCustomersClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Returns a paginated list of customers for the store. Use the <code>search</code> parameter to filter
     * by name, email, phone, card code (barcode), or external reference ID. Alternatively, pass
     * <code>source</code> AND <code>external_id</code> together to perform an exact lookup by an external reference -
     * the response will contain at most one customer.
     */
    public ListCustomersResponse list(int accountId) {
        return this.rawClient.list(accountId).body();
    }

    /**
     * Returns a paginated list of customers for the store. Use the <code>search</code> parameter to filter
     * by name, email, phone, card code (barcode), or external reference ID. Alternatively, pass
     * <code>source</code> AND <code>external_id</code> together to perform an exact lookup by an external reference -
     * the response will contain at most one customer.
     */
    public ListCustomersResponse list(int accountId, RequestOptions requestOptions) {
        return this.rawClient.list(accountId, requestOptions).body();
    }

    /**
     * Returns a paginated list of customers for the store. Use the <code>search</code> parameter to filter
     * by name, email, phone, card code (barcode), or external reference ID. Alternatively, pass
     * <code>source</code> AND <code>external_id</code> together to perform an exact lookup by an external reference -
     * the response will contain at most one customer.
     */
    public ListCustomersResponse list(int accountId, ListCustomersRequest request) {
        return this.rawClient.list(accountId, request).body();
    }

    /**
     * Returns a paginated list of customers for the store. Use the <code>search</code> parameter to filter
     * by name, email, phone, card code (barcode), or external reference ID. Alternatively, pass
     * <code>source</code> AND <code>external_id</code> together to perform an exact lookup by an external reference -
     * the response will contain at most one customer.
     */
    public ListCustomersResponse list(int accountId, ListCustomersRequest request, RequestOptions requestOptions) {
        return this.rawClient.list(accountId, request, requestOptions).body();
    }

    /**
     * Creates a new customer for the store. Requires <code>first_name</code> and at least one of <code>email</code> or <code>phone</code>.
     * Optionally enroll the customer in a loyalty card by passing <code>card_id</code>, and trigger delivery of
     * card links (email/SMS) by passing <code>send_card_links</code>. When a card with initial stamps is assigned,
     * those stamps are automatically applied as a welcome bonus.
     * <p>Pass <code>metadata</code> to attach arbitrary key/value data, and <code>external_references</code> to link the
     * customer to records in other systems (e.g. Square, Shopify). External references are upserted
     * by <code>(source, external_id)</code> so this endpoint is safe to call with the same references twice.</p>
     */
    public CreateCustomersResponse create(int accountId, CreateCustomersRequest request) {
        return this.rawClient.create(accountId, request).body();
    }

    /**
     * Creates a new customer for the store. Requires <code>first_name</code> and at least one of <code>email</code> or <code>phone</code>.
     * Optionally enroll the customer in a loyalty card by passing <code>card_id</code>, and trigger delivery of
     * card links (email/SMS) by passing <code>send_card_links</code>. When a card with initial stamps is assigned,
     * those stamps are automatically applied as a welcome bonus.
     * <p>Pass <code>metadata</code> to attach arbitrary key/value data, and <code>external_references</code> to link the
     * customer to records in other systems (e.g. Square, Shopify). External references are upserted
     * by <code>(source, external_id)</code> so this endpoint is safe to call with the same references twice.</p>
     */
    public CreateCustomersResponse create(
            int accountId, CreateCustomersRequest request, RequestOptions requestOptions) {
        return this.rawClient.create(accountId, request, requestOptions).body();
    }

    /**
     * Returns detailed information about a single customer, including all of their
     * enrolled loyalty cards with stamp progress and wallet pass URLs (<code>apple_wallet_url</code>
     * and <code>google_wallet_url</code>) for each card. Also includes <code>metadata</code> and
     * <code>external_references</code> so you can sync state with external systems.
     */
    public GetCustomersResponse get(int accountId, int id) {
        return this.rawClient.get(accountId, id).body();
    }

    /**
     * Returns detailed information about a single customer, including all of their
     * enrolled loyalty cards with stamp progress and wallet pass URLs (<code>apple_wallet_url</code>
     * and <code>google_wallet_url</code>) for each card. Also includes <code>metadata</code> and
     * <code>external_references</code> so you can sync state with external systems.
     */
    public GetCustomersResponse get(int accountId, int id, RequestOptions requestOptions) {
        return this.rawClient.get(accountId, id, requestOptions).body();
    }

    /**
     * Returns detailed information about a single customer, including all of their
     * enrolled loyalty cards with stamp progress and wallet pass URLs (<code>apple_wallet_url</code>
     * and <code>google_wallet_url</code>) for each card. Also includes <code>metadata</code> and
     * <code>external_references</code> so you can sync state with external systems.
     */
    public GetCustomersResponse get(int accountId, int id, GetCustomersRequest request) {
        return this.rawClient.get(accountId, id, request).body();
    }

    /**
     * Returns detailed information about a single customer, including all of their
     * enrolled loyalty cards with stamp progress and wallet pass URLs (<code>apple_wallet_url</code>
     * and <code>google_wallet_url</code>) for each card. Also includes <code>metadata</code> and
     * <code>external_references</code> so you can sync state with external systems.
     */
    public GetCustomersResponse get(int accountId, int id, GetCustomersRequest request, RequestOptions requestOptions) {
        return this.rawClient.get(accountId, id, request, requestOptions).body();
    }

    /**
     * Updates an existing customer's details. To add stamps or redeem rewards, use the
     * customer cards endpoints instead.
     * <p><code>metadata</code> is shallow-merged into the existing metadata. <code>external_references</code> are upserted
     * by <code>(source, external_id)</code> - to remove a reference, omit it from subsequent calls and use
     * a separate <code>DELETE</code> workflow (not yet exposed via API; manage in dashboard for now).</p>
     */
    public UpdateCustomersResponse update(int accountId, int id, UpdateCustomersRequest request) {
        return this.rawClient.update(accountId, id, request).body();
    }

    /**
     * Updates an existing customer's details. To add stamps or redeem rewards, use the
     * customer cards endpoints instead.
     * <p><code>metadata</code> is shallow-merged into the existing metadata. <code>external_references</code> are upserted
     * by <code>(source, external_id)</code> - to remove a reference, omit it from subsequent calls and use
     * a separate <code>DELETE</code> workflow (not yet exposed via API; manage in dashboard for now).</p>
     */
    public UpdateCustomersResponse update(
            int accountId, int id, UpdateCustomersRequest request, RequestOptions requestOptions) {
        return this.rawClient.update(accountId, id, request, requestOptions).body();
    }
}
